name: Tracker Schedule Check

"on":
  schedule:
    - cron: '*/5 * * * *'

permissions: {}

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          printf '[DEBUG-tracker-schedule] event=%s\n' "$GITHUB_EVENT_NAME"
          date -u +'%Y-%m-%dT%H:%M:%SZ'
